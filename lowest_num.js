// ### Task -1: 
// Find the lowest number in the array below.
const number = [167, 190, 120, 165, 137, 10];
const result = Math.min.apply(Math, number);
console.log('Array is a lowest Number:',result);
